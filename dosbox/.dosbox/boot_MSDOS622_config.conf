[autoexec]
# This file should be sourced in addition to the default configuration
imgmount d ~/DOS/boot/MSDOS622_C.img -t hdd
@echo off
echo Access to DOS root is restricted, because
echo allowing it would break immersion and
echo lack of LFN support would eventually break real things.